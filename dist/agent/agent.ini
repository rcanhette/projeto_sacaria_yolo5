[agent]
# Identificador do agente (livre; aparece nos logs)
id=teste # ID da TC no servidor central que este agente opera
tc_id=1
# URL do servidor central acessível a partir deste PC
central_url=http://localhost:8080
# Token opcional (autenticação do agente desativada por padrão)
token=

