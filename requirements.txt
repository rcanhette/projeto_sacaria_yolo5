# Projeto Sacaria YOLOv5  dependencias de runtime
# Observacao:
# - O YOLOv5 e carregado do diretorio local `third_party/yolov5` (sem pip do ultralytics/yolov5)
# - O PyTorch pode requerer instrucoes especificas por SO/CPU. Se houver erro ao instalar,
#   siga as instrucoes oficiais: https://pytorch.org/get-started/locally/

# Web/API
Flask>=2.3

# Banco de dados (PostgreSQL)
psycopg2-binary>=2.9

# Visao computacional / Numerico
opencv-python>=4.8
numpy>=1.26
Pillow>=10.3
PyYAML>=6.0
requests>=2.31
scipy>=1.11
tqdm>=4.66
psutil>=5.9
pandas>=1.1.4
seaborn>=0.11.0

# Excel (export de logs)
openpyxl>=3.1

# PyTorch (CPU por padrao). Ajuste conforme plataforma se necessario.
torch
torchvision

# Servidor WSGI e Servico Windows
waitress>=2.1
pywin32>=306; platform_system == "Windows"

# Dependencia necessaria para alguns modulos do YOLOv5 recente
ultralytics>=8.2.64
