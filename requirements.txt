# Projeto Sacaria YOLOv5 — dependências de runtime
# Observação:
# - O YOLOv5 é carregado do diretório local `third_party/yolov5` (sem pip do ultralytics/yolov5)
# - O PyTorch pode requerer instruções específicas por SO/CPU. Se houver erro ao instalar,
#   siga as instruções oficiais: https://pytorch.org/get-started/locally/

# Web/API
Flask>=2.3

# Banco de dados (PostgreSQL)
psycopg2-binary>=2.9

# Visão computacional / Numérico
opencv-python>=4.8
numpy>=1.26
Pillow>=10.3
PyYAML>=6.0
requests>=2.31
scipy>=1.11
tqdm>=4.66
psutil>=5.9
pandas>=1.1.4
seaborn>=0.11.0

# Excel (export de logs)
openpyxl>=3.1

# PyTorch (CPU por padrão). Ajuste conforme plataforma se necessário.
torch
torchvision

# Servidor WSGI e Serviço Windows
waitress>=2.1
pywin32>=306; platform_system == "Windows"

# Dependência necessária para alguns módulos do YOLOv5 recente
ultralytics>=8.2.64
